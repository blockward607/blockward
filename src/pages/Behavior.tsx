
import { BehaviorTracker } from "@/components/behavior/BehaviorTracker";

const Behavior = () => {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-bold gradient-text">Behavior Management</h1>
      <BehaviorTracker />
    </div>
  );
};

export default Behavior;
